<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match [demo-ci-proj].**>
  @type opensearch
  @log_level debug
  host opensearch
  port 9200
  user Admin
  password 7&kMHk/E}xoaD+2PEZKW
  logstash_format true
  index_name %{type}-%{+YYYY.MM.dd}
  compress gzip
</match>